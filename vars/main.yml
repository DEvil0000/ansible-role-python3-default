---
# from ubuntu 20 or debian 10 onwards we can use the package
python3_default_try_package_method: "{{ ansible_distribution == 'Ubuntu' and ansible_distribution_major_version >= '20' or ansible_distribution == 'Debian' and ansible_distribution_major_version >= '10' }}"
